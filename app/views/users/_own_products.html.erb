<div>
  <% if @fridge.present? %>
    <%= @fridge.each do |fridge| %>
	  <% if fridge.user_id == @user.id %>
	    <%= fridge.amount %>
	    <%= fridge.product.name %>
	    <%= fridge.product.marke %>
	    <%= fridge.product.ort %>
	  <% end %>
	<% end %>
  <% end %>
</div>